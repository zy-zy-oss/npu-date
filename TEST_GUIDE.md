# 问卷功能测试指南

## 🎯 快速开始

### 1. 启动开发服务器
```bash
cd npu-date-web
npm run dev
```
打开浏览器: http://localhost:3004

### 2. 登录流程
```
账号: test@nwpu.edu.cn
密码: 123456
```

### 3. 进入问卷
- 点击首页的"开始"按钮
- 或登录后直接访问 /questionnaire

## ✅ 完整测试流程

### 问卷所有题目

| 题号 | 题目 | 类型 | 必填 | 测试值 |
|------|------|------|------|--------|
| Q1 | 你的性别 | 单选 | ✅ | 男 |
| Q2 | 期望对象的性别 | 单选 | ✅ | 女 |
| Q3 | 年级 | 单选 | ✅ | 2022级 |
| Q4 | 专业 | 单选 | ✅ | 计算机科学与技术 |
| Q5 | 所在校区 | 单选 | ✅ | 长安校区 |
| Q6 | 身高(cm) | 滑块 | ❌ | 170 |
| Q7 | 兴趣爱好 | 多选 | ❌ | 篮球, 编程 |
| Q8 | 性格类型 | 单选 | ❌ | 开朗外向 |
| Q9 | 是否颜控 | 单选 | ❌ | 否,更看重内在 |
| Q10 | 在意对方的条件 | 多选 | ❌ | 性格, 兴趣爱好 |
| Q11 | 自我介绍 | 文本 | ✅ | 我是一个热爱编程的大学生 |
| Q12 | 理想对象描述 | 文本 | ❌ | 温柔体贴 |
| Q13 | 邮箱验证 | 特殊 | ✅ | 见下表 |

### 邮箱验证专项测试

#### ✅ 正常流程 (成功路径)
```
1. 邮箱输入:
   输入框中输入: test@nwpu.edu.cn
   
2. 验证按钮状态:
   ✅ 按钮应该可用 (文字: "发送验证码")
   
3. 点击发送验证码:
   ⏳ 按钮变为禁用 (文字: "发送中...")
   ⌛ 0.5秒后显示验证码输入界面
   
4. 倒计时显示:
   显示文本: "验证码已发送到 test@nwpu.edu.cn，请输入验证码"
   显示倒计时: "60s 后重新发送"
   
5. 倒计时递减:
   ⏱️ 从60秒开始逐秒递减
   ⏱️ 0秒时倒计时重置为60
   
6. 输入验证码:
   输入框中输入任意6位数字 (例: 123456)
   
7. 点击确认:
   📧 显示 alert: "验证成功！"
   ✅ 自动跳转到下一题 (Q1)
```

#### ❌ 错误场景

**场景1: 无效的邮箱**
```
输入: student@qq.com
预期: 
- "发送验证码"按钮禁用
- 鼠标悬停提示或按钮显示灰色
```

**场景2: 邮箱为空**
```
输入: (空)
预期:
- "发送验证码"按钮禁用
```

**场景3: 验证码位数不足**
```
输入: 12345 (只有5位)
预期:
- 点击"确认"后 alert: "请输入6位验证码"
- 保持在验证码输入界面
```

**场景4: 验证码位数过多**
```
输入: 1234567 (7位)
预期:
- 输入框自动限制为6位 (maxlength="6")
- 只能输入前6个数字
```

**场景5: 更换邮箱**
```
步骤:
1. 发送验证码到 test@nwpu.edu.cn
2. 在验证码输入界面点击"更换邮箱"
预期:
- 返回邮箱输入界面
- 邮箱输入框清空
- 验证码输入框清空
- 倒计时重置为60秒
```

## 🧪 交互测试

### 键盘操作
```
✅ 在邮箱输入框按 Enter:
   触发发送验证码
   
✅ 在验证码输入框按 Enter:
   返回邮箱输入界面 (不确认)
```

### 按钮状态
```
✅ 邮箱步骤:
   - 邮箱有效 → 按钮可用
   - 邮箱无效 → 按钮禁用
   - 发送中 → 按钮禁用 + "发送中..."
   
✅ 验证码步骤:
   - "确认"按钮始终可用
   - 倒计时中 (< 60s) → "更换邮箱"可用
```

### 响应式设计
```
✅ 桌面端 (> 1024px):
   - 问卷卡片居中
   - 按钮并排显示
   - 邮箱验证界面水平布局
   
✅ 平板端 (768px - 1024px):
   - 问卷卡片适应宽度
   - 按钮可能换行
   - 邮箱验证界面调整间距
   
✅ 手机端 (< 768px):
   - 邮箱输入框占满宽度
   - "发送验证码"按钮可能显示在下一行
   - 验证码输入框占满宽度
```

## 📊 测试检查表

### 问卷导航
- [ ] 点击"← 上一题"可以返回上一题
- [ ] 点击"下一题 →"会检查必填项
- [ ] 必填项未填时"下一题"按钮禁用
- [ ] 最后一题时显示"完成提交"按钮
- [ ] 进度条显示正确 (X / 13)
- [ ] 进度条随题目递进

### 数据验证
- [ ] 所有必填题均已正确填写时"完成提交"可用
- [ ] 任何必填题未填时"完成提交"禁用
- [ ] 邮箱验证必须完成 (6位数字已输入)

### 样式效果
- [ ] 页面背景为紫色渐变
- [ ] 问卷卡片为白底黑边
- [ ] 所有按钮均为手绘风格
- [ ] 悬停效果正常
- [ ] 点击动画正常

### 提交成功
- [ ] 所有数据填完后点击"完成提交"
- [ ] 显示 alert: "问卷提交成功！"
- [ ] 自动跳转到 /crush (心动列表页面)

## 🔍 常见问题排查

### 问题1: 邮箱输入框无法输入
**解决:**
```
1. 清除浏览器缓存
2. 刷新页面
3. 查看浏览器控制台是否有错误
```

### 问题2: 倒计时不递减
**解决:**
```
1. 检查浏览器JavaScript是否启用
2. 查看浏览器控制台错误
3. 尝试F12打开开发者工具观察
```

### 问题3: 验证成功后未跳转
**解决:**
```
1. 检查verify-btn的onClick是否触发
2. 检查路由是否正确配置
3. 查看浏览器控制台的错误信息
```

### 问题4: 样式显示不正确
**解决:**
```
1. 清除浏览器缓存: Ctrl+Shift+Delete
2. 硬刷新页面: Ctrl+Shift+R
3. 尝试不同浏览器
```

## 📝 回归测试清单

完成以下所有测试后，问卷功能可认为成功：

- [ ] 所有13题都能正确显示和交互
- [ ] 年级/专业/校区使用Radio按钮而非Picker
- [ ] 邮箱验证两步流程完整可用
- [ ] 倒计时从60秒正确递减
- [ ] 验证成功自动跳转下一题
- [ ] 邮箱只接受@nwpu.edu.cn域名
- [ ] 验证码严格要求6位数字
- [ ] 所有必填项验证正确
- [ ] 最后一题提交时所有必填项已验证
- [ ] 手机端响应式显示正常
- [ ] 样式和动画效果正常

---

**测试版本:** v1.0  
**最后更新:** 2026年3月19日